# Link the C math library (msvcrt on Windows, m on Linux)
apocrypha "msvcrt" mystery cos();

inscribe counter = 0;

chant counter < 3 {
    inscribe val = cos(counter);
    proclaim val;
    inscribe counter = counter + 1;
} amen
